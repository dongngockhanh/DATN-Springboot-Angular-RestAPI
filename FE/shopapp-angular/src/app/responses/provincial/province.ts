export interface Province {
    province_id: string; // hoặc có thể là number tùy thuộc vào dữ liệu trả về từ API
    province_name: string;
    // id: string; // hoặc có thể là number tùy thuộc vào dữ liệu trả về từ API
    // name: string;
}